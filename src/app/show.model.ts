export class Show {
  constructor (public title: string, public network: string, public description: string, public trailer: string) { }
}

export class Show2 {
  constructor (public title: string, public network: string, public description: string, public trailer: string) { }
}
